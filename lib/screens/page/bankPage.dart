import 'package:flutter/material.dart';

class BankPage extends StatelessWidget {

  static String id = 'bankPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
        title: Text ('Bank'),
        )
    );
  }
}